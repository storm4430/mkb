extends layout

block content
  div(style='margin-bottom:10px;')
    h2.ui.header.block
      i.book.icon
      .content
        |Справочник МКБ-10
        .sub.header Воспользуйтесь навигацией по классам или поиском
  each i in user.json_out.data
    p
      a(href='/groups/'+ i.id)  #{i.mkb_code}
      span(style='margin-left:5px')
        !{i.mkb_name}
  script.
      VK.init({apiId: 5767032, onlyWidgets: true});
      VK.Widgets.Like("vk_like", {
          type: "mini",
          verb: 1,
          pageTitle: "#{title}",
          pageImage: "http://mkb11.info/images/soc_logo.jpg"
      }, #{appId});
